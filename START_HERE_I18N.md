# 🚀 Быстрый старт мультиязычности

## За 30 секунд

```bash
# 1. Откройте приложение
http://localhost:3000

# 2. Нажмите на кнопку языка (English/Arabic) в хедере
# 3. Готово! 🎉
```

---

## Для разработчиков

### Добавить новый перевод

```javascript
// 1. Откройте src/js/i18n/translations.js
// 2. Найдите нужный раздел и добавьте ключ:

export const translations = {
  en: {
    myFeature: { text: "My Text" }  // ← добавьте сюда
  },
  ar: {
    myFeature: { text: "النص الخاص بي" }  // ← и сюда
  }
}

// 3. Используйте в HTML:
<div data-i18="myFeature.text">My Text</div>

// 4. Или в JavaScript:
import { i18n } from "./i18n/index.js"
const text = i18n.t("myFeature.text")
```

### Получить текущий язык

```javascript
i18n.getLanguage()  // "en" или "ar"
```

### Обновить интерфейс

```javascript
i18n.updateDOM()  // обновит все data-i18 элементы
```

---

## Структура файлов

```
src/js/i18n/
├── index.js           ← Модуль управления языком
└── translations.js    ← Все переводы (en + ar)
```

---

## Где используется i18n?

| Файл | Что переводится |
|------|-----------------|
| main.js | Заголовок приложения, ошибки |
| render.js | Таймер, статус |
| header.html | Языки, звук |
| footer.html | Кнопка Share |
| share-popup.html | Все тексты popup |

---

## 📖 Документация

- **I18N_QUICK_START.md** — Краткое руководство
- **I18N.md** — Полная документация
- **I18N_SUMMARY.md** — Итоговый отчет

---

## Проверка работы

```bash
# Посмотреть localStorage
localStorage.language     # "en" или "ar"
localStorage.selectedLocation  # выбранный город

# Проверить в браузере
i18n.t("app.title")      # Получить перевод
i18n.getLanguage()       # Текущий язык
i18n.setLanguage("ar")   # Сменить язык
```

---

## 💡 Советы

1. **Используйте точечную нотацию для ключей:**
   ```javascript
   ✅ i18n.t("section.subsection.key")
   ❌ i18n.t("section-subsection-key")
   ```

2. **Всегда добавляйте переводы на оба языка:**
   ```javascript
   ✅ { en: {...}, ar: {...} }
   ❌ { en: {...} }  // Неполно!
   ```

3. **Используйте `data-i18` в HTML для простоты:**
   ```html
   ✅ <div data-i18="key">Default</div>
   ❌ <div id="text"></div>  <!-- Потом придётся i18n.t() -->
   ```

---

## 🆘 Проблемы?

### Язык не сохраняется
```javascript
// Проверьте localStorage
console.log(localStorage.getItem("language"))
```

### RTL не работает
```html
<!-- Убедитесь, что установлен Arabic -->
<html lang="ar" dir="rtl">
```

### Перевод не обновляется
```javascript
// Вызовите updateDOM()
i18n.updateDOM()
```

---

## ✨ Готово!

Теперь у вас полная поддержка мультиязычности! 🌍

Вопросы? Смотрите **[I18N.md](I18N.md)**
