# Мультиязычность - Краткая инструкция

## ✅ Что реализовано

- **2 языка**: Английский (en) и Арабский (ar)
- **Системы перевода (i18n)**: все строки интерфейса переведены
- **Автоматическое сохранение**: выбранный язык сохраняется в `localStorage`
- **RTL поддержка**: при выборе арабского макет автоматически переводится в режим RTL
- **Динамическое обновление**: весь интерфейс обновляется при смене языка

## 🎯 Использование

### Для пользователя
1. Нажмите на кнопку **English** или **Arabic** в хедере
2. Весь интерфейс переведется на выбранный язык
3. Язык сохранится при перезагрузке страницы

### Для разработчика

#### Добавить текст с переводом в HTML
```html
<div data-i18="section.key">English text here</div>
```

#### Добавить перевод в JavaScript
```javascript
import { i18n } from "./i18n/index.js"
const text = i18n.t("section.key")  // Получит текст на текущем языке
```

#### Добавить новый перевод
1. Откройте [src/js/i18n/translations.js](src/js/i18n/translations.js)
2. Добавьте ключ на оба языка:
```javascript
export const translations = {
  en: {
    myNew: { text: "My Text" }
  },
  ar: {
    myNew: { text: "نصي" }
  }
}
```
3. Используйте: `data-i18="myNew.text"` или `i18n.t("myNew.text")`

## 📂 Структура файлов i18n

```
src/js/i18n/
├── index.js           # Логика переключения языков
└── translations.js    # Словарь переводов (en + ar)
```

## 🌐 Текущие переводы

| Раздел | Ключи |
|--------|-------|
| **app** | `title` |
| **dropdown** | `placeholder` |
| **footer** | `btn` |
| **popup** | `header`, `title.day`, `title.night`, `copyBtn` |
| **language** | `en`, `ar` |
| **sound** | `play`, `mute` |
| **timer** | `complete`, `iftar` |
| **error** | `cityNotFound` |

## 💾 LocalStorage

Два параметра сохраняются автоматически:
- **`language`**: текущий язык (`"en"` или `"ar"`)
- **`selectedLocation`**: выбранный город (из dropdown)

## 📖 Подробная документация

Все детали в файле [I18N.md](I18N.md)

## 🚀 Тестирование

1. Откройте http://localhost:3000
2. Кликните на English/Arabic кнопке в хедере
3. Проверьте:
   - ✅ Все тексты переведены
   - ✅ Макет переходит в RTL при Arabic
   - ✅ Язык сохраняется при перезагрузке
