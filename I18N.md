# Интернационализация (i18n) — Документация

## Обзор

Приложение поддерживает полную интернационализацию с двумя языками:
- 🇬🇧 **English** (Английский)
- 🇸🇦 **العربية** (Арабский)

Выбор языка сохраняется в `localStorage` и автоматически восстанавливается при загрузке страницы.

---

## Структура i18n

### Файлы проекта

```
src/js/i18n/
├── index.js           # Основной модуль i18n
└── translations.js    # Словарь переводов
```

### Ключевые компоненты

#### 1. **translations.js** — Словарь переводов

Содержит объект со всеми переводами на оба языка:

```javascript
export const translations = {
  en: {
    app: { title: "Fasting Time" },
    dropdown: { placeholder: "Select location" },
    // ...
  },
  ar: {
    app: { title: "وقت الصيام" },
    dropdown: { placeholder: "اختر الموقع" },
    // ...
  }
}
```

**Структура ключей:**
- Используется точечная нотация: `"app.title"`, `"popup.header"`
- Вложенные объекты для логической группировки

#### 2. **index.js** — Модуль управления языком

Основные методы:

- `i18n.init()` — инициализация языка (загружает из localStorage)
- `i18n.setLanguage(lang)` — установить язык (`"en"` или `"ar"`)
- `i18n.getLanguage()` — получить текущий язык
- `i18n.t(key)` — получить перевод по ключу
- `i18n.updateDOM()` — обновить все элементы с `data-i18` атрибутом

---

## Как использовать i18n

### 1. В HTML (декларативный способ)

Используйте атрибут `data-i18` с ключом перевода:

```html
<!-- Footer кнопка -->
<div class="footer__btn" data-i18="footer.btn">Share Journey</div>

<!-- Языки в хедере -->
<div data-i18="language.en">English</div>
<div data-i18="language.ar">Arabic</div>

<!-- Звук -->
<div data-i18="sound.play">Play</div>
<div data-i18="sound.mute">Mute</div>

<!-- Popup -->
<div data-i18="popup.header">Share Journey</div>
<div data-i18="popup.title.day">Fasting Today</div>
<div data-i18="popup.title.night">Fasting Completed</div>
```

### 2. В JavaScript (программный способ)

```javascript
import { i18n } from "./i18n/index.js"

// Инициализация
i18n.init()

// Получить перевод
const title = i18n.t("app.title") // "Fasting Time" или "وقت الصيام"

// Установить новый язык
i18n.setLanguage("ar")

// Обновить DOM
i18n.updateDOM()
```

### 3. Интеграция в компоненты

Пример из [render.js](src/js/ui/render.js):

```javascript
import { i18n } from "../i18n/index.js"

export function renderTimer(valueOrMode) {
  const titleEl = document.querySelector(".app__title")
  
  if (valueOrMode === "night-complete") {
    titleEl.textContent = i18n.t("timer.iftar")  // "Iftar Time"
    timerEl.textContent = i18n.t("timer.complete")  // "May your fast..."
    return
  }
  
  titleEl.textContent = i18n.t("app.title")  // "Fasting Time"
}
```

---

## Структура переводов

### Текущие разделы

| Ключ | Описание | Пример |
|------|---------|--------|
| `app.title` | Заголовок приложения | "Fasting Time" |
| `dropdown.placeholder` | Плейсхолдер dropdown | "Select location" |
| `footer.btn` | Кнопка в footer | "Share Journey" |
| `popup.header` | Заголовок popup | "Share Journey" |
| `popup.title.day` | Заголовок popup (день) | "Fasting Today" |
| `popup.title.night` | Заголовок popup (ночь) | "Fasting Completed" |
| `popup.copyBtn` | Кнопка копирования | "Copy Image" |
| `language.en` | Название языка English | "English" |
| `language.ar` | Название языка Arabic | "Arabic" |
| `sound.play` | Кнопка звука (вкл) | "Play" |
| `sound.mute` | Кнопка звука (выкл) | "Mute" |
| `timer.complete` | Сообщение завершения поста | "May your fast..." |
| `timer.iftar` | Время для iftar | "Iftar Time" |
| `error.cityNotFound` | Ошибка города не найден | "City not found" |

---

## Добавление нового перевода

### Шаг 1: Добавить ключ в translations.js

```javascript
export const translations = {
  en: {
    // ... существующие ключи
    newFeature: {
      title: "My New Feature",
      description: "This is new"
    }
  },
  ar: {
    // ... существующие ключи
    newFeature: {
      title: "ميزتي الجديدة",
      description: "هذا جديد"
    }
  }
}
```

### Шаг 2: Использовать в HTML

```html
<div data-i18="newFeature.title">My New Feature</div>
```

Или в JavaScript:

```javascript
const text = i18n.t("newFeature.description")
```

---

## Обработчик смены языка

Интеграция в [main.js](src/js/main.js):

```javascript
document.querySelectorAll(".lang-btn").forEach(btn => {
  btn.addEventListener("click", () => {
    const lang = btn.getAttribute("data-lang")
    i18n.setLanguage(lang)  // Меняет язык + сохраняет в localStorage
    
    // Обновляем активную кнопку
    document.querySelectorAll(".lang-btn").forEach(b => b.classList.remove("selected"))
    btn.classList.add("selected")
    
    // Обновляем DOM
    i18n.updateDOM()
  })
})
```

---

## Локальное хранилище

Язык автоматически сохраняется в `localStorage`:

```javascript
localStorage.getItem("language")  // "en" или "ar"
localStorage.getItem("selectedLocation")  // "Dubai, UAE"
```

При загрузке страницы язык восстанавливается автоматически.

---

## RTL поддержка (для арабского)

При установке арабского языка автоматически:

1. Устанавливается `dir="rtl"` на элементе `<html>`
2. Браузер переводит макет справа налево

```javascript
i18n.updatePageLanguage()  // Автоматически вызывается при setLanguage
```

---

## Примеры использования

### Пример 1: Динамический текст

```javascript
const city = "Dubai"
const errorMsg = `${i18n.t("error.cityNotFound")} "${city}"`
// результат: "City not found "Dubai""
// или: "لم يتم العثور على المدينة "Dubai""
```

### Пример 2: Условный рендер

```javascript
if (i18n.getLanguage() === "ar") {
  // Специфичный код для арабского
}
```

### Пример 3: Вложенные ключи

```javascript
// Доступ к вложенным переводам
i18n.t("popup.title.day")    // "Fasting Today"
i18n.t("popup.title.night")  // "Fasting Completed"
```

---

## Файлы, интегрированные с i18n

- ✅ [main.js](src/js/main.js) — инициализация и обработчик языка
- ✅ [render.js](src/js/ui/render.js) — рендер таймера и цитаты
- ✅ [dropdown.js](src/js/ui/dropdown.js) — импортирован i18n
- ✅ [location.js](src/js/api/location.js) — сообщения об ошибках
- ✅ [header.html](src/components/header.html) — языки и звук
- ✅ [footer.html](src/components/footer.html) — кнопка Share Journey
- ✅ [share-popup.html](src/components/share-popup.html) — текст popup

---

## Тестирование

1. Откройте приложение: http://localhost:3000
2. Нажмите на кнопку языка (English/Arabic) в хедере
3. Проверьте, что:
   - Весь текст переводится
   - Макет переходит в RTL при арабском языке
   - Язык сохраняется при перезагрузке страницы

---

## Структура локализации

```
Английский (en):
├── Интерфейс: English
├── Направление: LTR (слева направо)
└── По умолчанию при первом визите

Арабский (ar):
├── Интерфейс: العربية
├── Направление: RTL (справа налево)
└── При выборе из dropdown
```

---

## Дополнительные ресурсы

- Словарь переводов: [translations.js](src/js/i18n/translations.js)
- Модуль i18n: [index.js](src/js/i18n/index.js)
- Примеры использования в [main.js](src/js/main.js)
